// import { Capacitor } from '@capacitor/core';

// // Ensure that Capacitor is initialized before using any plugins or features
// async function initCapacitor() {
//   if (!(await Capacitor.isInitialized())) {
//     await Capacitor.init();
//   }
// }

// // Call initCapacitor to ensure Capacitor is initialized
// initCapacitor()
//   .then(() => {
//     // Capacitor is now initialized and ready to use
//     console.log('Capacitor initialized');
//   })
//   .catch(error => {
//     console.error('Error initializing Capacitor:', error);
//   });
